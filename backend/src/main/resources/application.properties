server.port=8080

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:banking_app}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER:bankapp}
spring.datasource.password=${MYSQL_PASSWORD:Password123$}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
logging.level.org.springframework.security=TRACE
dger.fabric.org.channel=${CHANNEL_NAME:mainchannel}
hyperledger.fabric.org.cc-name=${CHAINCODE_NAME:bank-transactions}
hyperledger.fabric.org.peer-endpoint=${PEER_ENDPOINT:localhost:7051}
hyperledger.fabric.org.override-auth=${OVERRIDE_AUTH:peer0.org1.examplcors.allowed-origins=${ALLOWED_ORIGIN:http://localhost:4200}
hyperledger.fabric.org.msp=${MSP_ID:Org1MSP}
hyperlee.com}
springfox.documentation.swagger-ui.base-url=/documentation
